# Orchestration Log: apoc Agents
**Timestamp:** 2026-05-11T23:13:40Z

## apoc-1: Live-call Playwright Testing

**Status:** Completed (commit 396b3ee)

**Work Summary:**
- Discovered Foundry Local not running on port 5273 during live exploratory testing with Playwright.
- Fixed error handling in Program.cs: `/v1/chat/completions` proxy now returns 503 Service Unavailable (not 500) when upstream is unreachable.
- Added user-readable `detail` field to ProblemDetails response: "Could not reach Foundry Local at {endpoint}. Ensure the service is running."
- Updated App.tsx frontend to parse ProblemDetails and display the detail/title field in error messaging.

**Rationale:** 503 correctly signals upstream dependency outage (not application bug). Surfacing the endpoint URL in error message immediately directs developers to the problem source.

**Impact:** Error handling now provides actionable diagnostics for users and developers when Foundry Local is unavailable.

---

## apoc-2: Configured opencode v1.14.31 to Use Local Foundry LLM Server

**Status:** Completed (commit 14d7f19)

**Work Summary:**
- Added `GET /v1/models` endpoint to Program.cs to satisfy opencode's SDK model discovery requirements.
- Configured opencode v1.14.31 to use local Foundry LLM server as custom `foundry-local` provider at `http://localhost:5537/v1`, targeting `phi-4` model.
- Added foundry-local provider entry to `~/.config/opencode/opencode.json` (user-level global config, not repository-tracked).

**Rationale:** Allows developers to use opencode's AI-powered code generation without external API keys, leveraging the project's local Foundry Local GPU inference server.

**Impact:** `opencode run --model foundry-local/phi-4 "prompt"` now works locally. Enables local LLM-powered development workflows within the project environment.

---

## Coordination Notes

Both agents completed successfully. Decisions have been merged into squad/decisions.md. All critical error paths are now covered for proxy-based inference endpoints.
