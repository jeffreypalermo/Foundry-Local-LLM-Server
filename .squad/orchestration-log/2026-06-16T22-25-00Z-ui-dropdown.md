# Orchestration Log — ui-dropdown

**Session:** Multi-Model Support (2026-06-16)  
**Agent:** ui-dropdown (Trinity)  
**Status:** Completed (background)

## Summary

Trinity added a React model-selector dropdown to the frontend UI, wired to Tank's new `GET /api/models` and `POST /api/models/select` endpoints for runtime model switching.

## Deliverables

- `<select id="model-select">` dropdown in new `div.model-picker`
- Options populated from `GET /api/models` on mount, annotated with ` • loaded` / ` • cached` status
- On selection: `POST /api/models/select { model }` with loading state ("Switching model… (unloading + loading on GPU)")
- Success: update `activeModel`, `models[].active` flags, `config.model`
- Error: parse RFC7807 ProblemDetails, display in existing `p.error`, revert select to last-active
- All Playwright-critical selectors preserved (`p.config-line > strong`, article.message selectors, p.error, p.config-line)

## Verification

- Frontend build: `npm run build` (tsc + vite) 0 errors
- Linting: `npx eslint src` clean
- TypeScript strict mode enabled
- Pre-existing 2 unused-var errors in `frontend/tests/*.spec.ts` left untouched

## Decision Record

Decision #11 (`trinity-model-dropdown.md`)
